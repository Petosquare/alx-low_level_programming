#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to be multiplied by first number
 * Return: The result of multiplication between x and y
 */

int mul(int x, int y)

{
	return (x * y);
}
